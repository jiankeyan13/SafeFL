# main_vanilla_flame.py - FLAME 防御 + BadNets 攻击
experiment_name: flame
logging:
  project: SafeFL_Vanilla
  use_wandb: true

model:
  name: resnet18
  params:
    num_classes: 10
    input_channels: 3

dataset: cifar10

algorithm:
  name: flame

attack:
  enabled: true
