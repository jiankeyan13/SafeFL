# main_vanilla.py - FedAvg + BadNets 攻击
experiment_name: fedavg
logging:
  project: SafeFL_Vanilla
  use_wandb: true

model:
  name: resnet18
  params:
    num_classes: 10
    input_channels: 3

dataset: cifar10

algorithm:
  name: fedavg

attack:
  enabled: true
  malicious_epochs: 5
