# main_vanilla_deepsight.py - DeepSight 防御 + BadNets 攻击
experiment_name: vanilla_deepsight_cifar10_resnet18
logging:
  project: FL_Base
  use_wandb: true

model:
  name: resnet18
  params:
    num_classes: 10
    input_channels: 3

dataset: cifar10

algorithm:
  name: deepsight

attack:
  enabled: true
