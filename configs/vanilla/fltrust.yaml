# main_vanilla_fltrust.py - FLTrust + BadNets 攻击
experiment_name: fltrust
logging:
  project: SafeFL_Vanilla
  use_wandb: true

model:
  name: resnet18
  params:
    num_classes: 10
    input_channels: 3

dataset: cifar10

algorithm:
  name: fltrust

attack:
  enabled: true
