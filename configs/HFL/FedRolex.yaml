# main_hfl.py - 异构联邦学习 (HFL) FedRoLex
# model/algorithm/hetero 使用 extension/HFL/config.py 默认
experiment_name: fedrolex
dataset: cifar10
device: cuda:0
logging:
  project: HFL_experiment1
  use_wandb: true

attack:
  enabled: true

hetero:
  attacker:
    enabled: true
