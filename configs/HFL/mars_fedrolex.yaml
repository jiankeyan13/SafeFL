# main_hfl.py - 异构联邦学习 (HFL) MARS + FedRoLex
# 实验配置: epoch=2, rounds=150, malicious_epochs=5
experiment_name: mars_fedrolex
dataset: cifar10
epochs: 2
rounds: 150
device: cuda:2

logging:
  project: HFL_experiment1
  use_wandb: true

algorithm:
  name: mars_fedrolex

attack:
  enabled: true
  malicious_epochs: 5

hetero:
  attacker:
    enabled: true
